falis(gh-631):ARGF.readline reads one line of a file
falis(gh-631):ARGF.readline reads all lines of a file
falis(gh-631):ARGF.readline reads all lines of stdin
falis(gh-631):ARGF.readline reads all lines of two files
falis(gh-631):ARGF.readline sets $_ global variable with each line read
falis(gh-631):ARGF.readline modifies the files when in place edit mode is on
falis(gh-631):ARGF.readline modifies and backups two files when in place edit mode is on
falis(gh-631):ARGF.readline raises an EOFError when reaching end of files
