windows:ARGF.each_byte yields each byte of all streams to the passed block
windows:ARGF.each_byte returns an Enumerator when passed no block
windows:ARGF.each_byte when no block is given returns an Enumerator
